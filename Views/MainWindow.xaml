<Window x:Class="MagazinWPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Магазин — Каталог"
        Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="24,16">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🛍" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <StackPanel>
                        <TextBlock Text="Магазин" FontSize="18" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Каталог товарів" FontSize="12" Foreground="#D8F3DC"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock x:Name="UserNameTextBlock" Foreground="White"
                               VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <Button Content="Вийти" Style="{StaticResource SecondaryButton}" Click="LogoutButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource ItemCardStyle}" Margin="0,0,16,0" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Категорії" Style="{StaticResource SectionHeader}"/>
                    <ListBox x:Name="CategoriesListBox" BorderThickness="0" Background="Transparent"
                             SelectionChanged="CategoriesListBox_SelectionChanged">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="Margin" Value="0,2"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Bd" Background="Transparent" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                                                    <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#F1F3F5"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource ItemCardStyle}" Margin="0,0,16,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Товари" Style="{StaticResource SectionHeader}"/>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="ProductsItemsControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="170" Background="#FAFBFC" CornerRadius="12"
                                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                            Padding="12" Margin="0,0,12,12">
                                        <StackPanel>
                                            <Border Height="100" Background="#F1F3F5" CornerRadius="10" Margin="0,0,0,10">
                                                <TextBlock Text="📦" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>

                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"
                                                       TextWrapping="Wrap" Margin="0,0,0,6" MinHeight="36"/>

                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                <TextBlock Text="{Binding Price, StringFormat=N0}"
                                                           Foreground="{StaticResource PrimaryBrush}" FontWeight="Bold" FontSize="15"/>
                                                <TextBlock Text=" грн"
                                                           Foreground="{StaticResource PrimaryBrush}" FontWeight="Bold" FontSize="15"/>
                                            </StackPanel>

                                            <TextBlock Margin="0,0,0,10" FontSize="11"
                                                       Foreground="{StaticResource MutedTextBrush}">
                                                <Run Text="В наявності: "/>
                                                <Run Text="{Binding Stock}"/>
                                                <Run Text=" шт."/>
                                            </TextBlock>

                                            <Button Content="Купити" Style="{StaticResource PrimaryButton}"
                                                    Tag="{Binding}" Click="AddToCart_Click"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource ItemCardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Кошик" Style="{StaticResource SectionHeader}"/>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="CartItemsControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#FAFBFC" CornerRadius="10"
                                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                            Padding="10" Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Product.Name}" FontSize="13" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                    <TextBlock Text="{Binding TotalPrice, StringFormat=N0}" FontSize="12" Foreground="{StaticResource MutedTextBrush}"/>
                                                    <TextBlock Text=" грн" FontSize="12" Foreground="{StaticResource MutedTextBrush}"/>
                                                </StackPanel>
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                <Button Content="−" Style="{StaticResource RoundIconButton}"
                                                        Tag="{Binding}" Click="DecreaseQuantity_Click"/>
                                                <TextBlock Text="{Binding Quantity}" FontWeight="SemiBold"
                                                           Width="28" TextAlignment="Center" VerticalAlignment="Center"/>
                                                <Button Content="+" Style="{StaticResource RoundIconButton}"
                                                        Tag="{Binding}" Click="IncreaseQuantity_Click"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <TextBlock Grid.Row="2" x:Name="EmptyCartTextBlock" Text="Кошик порожній"
                               Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center"
                               Margin="0,8" Visibility="Visible"/>

                    <StackPanel Grid.Row="3" Margin="0,12,0,0">
                        <Separator Margin="0,0,0,12"/>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="Разом" Foreground="{StaticResource MutedTextBrush}" FontSize="13"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock x:Name="TotalAmountTextBlock" Text="0" FontWeight="Bold" FontSize="18"/>
                                <TextBlock Text=" грн" FontWeight="Bold" FontSize="18"/>
                            </StackPanel>
                        </Grid>
                        <Button Content="Оформити замовлення" Style="{StaticResource PrimaryButton}" Click="Checkout_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>